version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: freestays-api
    ports:
      - "5240:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080
      - ConnectionStrings__DefaultConnection=Host=3.72.175.63;Port=4848;Database=freestays;Username=usrarvas;Password=Barneveld2026
      - ConnectionStrings__Redis=freestays-cachedb-aucb6o:6379,password=Barneveld2026,ssl=false,abortConnect=false
    volumes:
      - ./logs:/app/logs
      - ./wwwroot/media:/app/wwwroot/media
      - ./wwwroot/uploads:/app/wwwroot/uploads
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8080/health" ]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 40s
    networks:
      - freestays-network

networks:
  freestays-network:
    driver: bridge
