# 🔐 FreeStays API - Environment Variables Template
# Copy this to .env and fill with actual values in your deployment environment
# NEVER commit .env file to git!

# ============================================
# DATABASE CONFIGURATION
# ============================================
# PostgreSQL connection string
# Format: Host=<IP>;Port=<PORT>;Username=<USER>;Password=<PASS>;Database=<DB>
ConnectionStrings__DefaultConnection=Host=3.72.175.63;Port=4848;Username=usrarvas;Password=YOUR_DB_PASSWORD_HERE;Database=freestays

# ============================================
# REDIS CONFIGURATION (Cache only, NOT for Hangfire jobs)
# ============================================
# Redis connection string
# Format: <HOST>:<PORT>,password=<PASSWORD>,defaultDatabase=0,...
# ⚠️ IMPORTANT: NO allowAdmin=true for security
# ⚠️ IMPORTANT: Set maxmemory limit in Redis server config (256mb recommended)
ConnectionStrings__Redis=3.72.175.63:6379,password=YOUR_REDIS_PASSWORD_HERE,defaultDatabase=0,ssl=false,abortConnect=false

# ============================================
# JWT SETTINGS
# ============================================
# Secret key for JWT token signing
# Generate with: openssl rand -hex 32
JwtSettings__Secret=YOUR_JWT_SECRET_KEY_HERE_MIN_32_CHARS_RECOMMENDED

# JWT token issuer
JwtSettings__Issuer=FreeStays.API

# JWT token audience
JwtSettings__Audience=FreeStays.Client

# Token expiration in minutes (1 hour recommended for production)
JwtSettings__ExpirationInMinutes=60

# Refresh token expiration in days (7 days recommended)
JwtSettings__RefreshTokenExpirationInDays=7

# ============================================
# HANGFIRE CONFIGURATION
# ============================================
# Number of background job workers
# Recommended: 2 for 2GB RAM server, 4-8 for larger servers
Hangfire__WorkerCount=2

# Maximum number of jobs to keep in history (memory optimization)
Hangfire__MaxJobHistoryCount=500

# Maximum number of automatic retry attempts
Hangfire__MaxRetryAttempts=2

# Automatic retry attempts per job
Hangfire__AutomaticRetryAttempts=1

# Size of failed jobs list (history)
Hangfire__FailedListSize=1000

# Size of deleted jobs list (history)
Hangfire__DeletedListSize=2000

# Size of succeeded jobs list (history)
Hangfire__SucceededListSize=2000

# ============================================
# CORS CONFIGURATION
# ============================================
# Allowed origins for CORS (comma-separated)
Cors__AllowedOrigins__0=https://freestays.eu
Cors__AllowedOrigins__1=https://admin.freestays.eu

# ============================================
# FILE UPLOAD CONFIGURATION
# ============================================
# Maximum file size in MB
FileUpload__MaxFileSizeInMB=5

# ============================================
# SUNHOTELS API CONFIGURATION
# ============================================
# SunHotels API base URL
SunHotels__BaseUrl=http://xml.sunhotels.net/15/PostGet/NonStaticXMLAPI.asmx

# SunHotels API username
SunHotels__Username=YOUR_SUNHOTELS_USERNAME

# SunHotels API password
SunHotels__Password=YOUR_SUNHOTELS_PASSWORD

# ============================================
# ASPNETCORE SETTINGS
# ============================================
# Allowed hosts (comma-separated)
ASPNETCORE_ALLOWED_HOSTS=*

# Environment: Development, Staging, Production
ASPNETCORE_ENVIRONMENT=Production

# HTTPS port (usually 443)
ASPNETCORE_HTTPS_PORT=443

# HTTP port (usually 80, disabled in production)
ASPNETCORE_HTTP_PORT=80

# ============================================
# DEPLOYMENT IN DOKPLOY
# ============================================
# To use these environment variables in Dokploy:
# 1. Go to Application Settings → Environment Variables
# 2. Copy each variable above (e.g., ConnectionStrings__DefaultConnection)
# 3. Paste the key name and actual value from your secrets
# 4. Save and redeploy
#
# Example in Dokploy UI:
# Key: ConnectionStrings__DefaultConnection
# Value: Host=3.72.175.63;Port=4848;Username=usrarvas;Password=actual_password;Database=freestays

# ==================================================================================
# ASP.NET CORE
# ==================================================================================
ASPNETCORE_ENVIRONMENT="Production"
ASPNETCORE_URLS="http://+:5000"

# ==================================================================================
# LOGGING
# ==================================================================================
# Mevcut ayarlar appsettings.json'da yönetiliyor
# Serilog otomatik olarak yüklenecek
# ==================================================================================

# ==================================================================================
# NOTLAR:
# ==================================================================================
# 1. Dokploy'de "Environment Variables" sekmesine yukarıdaki değişkenleri ekle
# 2. Password'lerdeki özel karakterler düz yazılmalı (URL encoding yok!)
# 3. ConnectionStrings için double-quote kullan
# 4. Local development'te appsettings.Development.json kullanılır (bu dosya ignore'lanmamış)
# 5. Push/deploy sırasında bağlantı string'ini değiştirmek gerekmiyor
# ==================================================================================
